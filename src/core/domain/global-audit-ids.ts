import { GLOBAL_AUDIT_ORG_ID } from "./platform-audit-actions";

/** Placeholder organization id for super-admin actions not tied to a real organization. */
export const GLOBAL_PROMPT_AUDIT_ORG_ID = GLOBAL_AUDIT_ORG_ID;
